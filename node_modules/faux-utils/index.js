var helper = require('./lib/helpers');

function add(a, b) {
  return helper.sum(a, b);
}

function addAgain(a, b) {
  return helper.sum(a, b);
}

module.exports = {
  add: add,
  addAgain: addAgain
};
